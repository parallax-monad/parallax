# Read-only Monad RPC endpoint for the Kuru live adapter. Never signed, never
# broadcast. The value is intentionally not read from a file by the smoke.
MOSS_RPC_URL=

# Absolute path to a pinned Moss checkout with built workspace packages
# (e.g. a worktree checked out at the exact runtime revision).
MOSS_RUNTIME_PATH=
